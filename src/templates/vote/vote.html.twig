{% extends 'base.html.twig' %}

{% block body %}
	<div class='col'>
		<form action="{{path('app_user_vote', {'id': user.id})}}" methods:'post'>
			<button name="direction" value="up">Vote up</button>
			<button name="direction" value="down">Vote down</button>
			<span>{{ user.votesString }}</span>
		</form>
	</div>
{% endblock %}